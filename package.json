{
  "name": "NoC-Performance-Dashboard",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "1.0.6"
  },
  "dependencies": {
    "react": "next",
    "react-dom": "next",
    "react-image-file": "^0.1.0",
    "victory": "0.22.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "react-scripts build",
    "deploy": "rsync -avze ssh --delete --exclude '*.map' build/ host:public_html/nocperfdashboard/"
  }
}
